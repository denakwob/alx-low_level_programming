#include "main.h"
/**
 * print_last_digit - prints the last digit of a number
 * @a: argument
 *
 * Return: a on succces
 *
 * Author - Nana Obeng;
 */
int print_last_digit(int a)
{
	int num;
	num = a % 10;

	if ( a <= 0)
	{
		a = a * -1;
		return (num);
	}
	else 
	{
		return (num);
	}
	return (num);
}
